[build]
  base = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  VITE_API_BASE_URL = "https://raid-nexus.onrender.com"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
